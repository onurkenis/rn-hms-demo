import { RNErrorEnum } from 'react-native-hwpush';

export const DEFAULT_LOG = `--- Push Token log ---\n`;
export const DEFAULT_TOPIC = `DEFAULT_TOPIC`;
export const RESULT_CODE_SUCCESS = RNErrorEnum.SUCCESS;
